package org.firstinspires.ftc.teamcode.powerplay;

public class TeleOp {
    /*
    the teleOp file for the robot
    use linearOpMode for this
    use all the implementations to recreate lemTeleOp2
    try to use liams funky state machine logic

    while(opModeIsRunning) {
        if(controls aren't where they were last time)
            change the thing
        if(other controls aren't where they were)
            change the thing
    }
     */

}
